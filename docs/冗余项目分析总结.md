# Amadeus Py 冗余项目分析总结

分析日期：2026-08-20  
分析范围：项目根目录、Python/UI 源码、资源、测试、构建配置、本地运行目录。  
本次只做盘点和建议，**没有删除任何文件或目录**。

## 一、结论摘要

项目同时包含桌宠应用、测试代码、DeepSeek Harness SDK、GPT-SoVITS 程序和模型、Live2D 资源、构建产物、缓存、设计稿及开发记录。建议先清理本地生成物，再处理确认没有运行引用的旧资源；核心依赖、模型和文档不能直接删除。

## 二、可优先清理的本地生成物

以下目录已被 `.gitignore` 标记为缓存、虚拟环境、构建产物或运行状态，通常可以清理，但仍应先确认是否需要保留本地状态：

| 项目 | 判断 | 建议 |
|---|---|---|
| `build/` | PyInstaller 构建中间产物 | 可清理，需要发布时重新构建 |
| `dist/` | PyInstaller 发布输出 | 确认发布包已备份后再清理 |
| `.venv/` | 项目 Python 虚拟环境 | 可按 `requirements.txt` 重建 |
| `gpt_sovits_venv/`、`gpt_sovits_venv_py311/` | GPT-SoVITS 虚拟环境 | 先确认实际使用的是哪一个 |
| `.pnpm-store/`、`node_modules/` | Node/Harness 依赖目录 | 不在本机编译 Harness 时可清理 |
| `.pytest_cache/`、`__pycache__/` | 测试和 Python 缓存 | 可直接清理 |
| `.sessions/` | 本地会话状态 | 先确认是否需要保留历史会话 |
| `crash.log`、`probe4.log`、`probe4_err.log` | 临时日志，其中两个为空 | 保留故障信息后可清理 |

这些内容属于本地生成物，不等于业务功能无用。清理后通常不影响源码，但可能影响启动速度、历史会话或已有安装包。

## 三、疑似冗余资源：需要确认后处理

### 1. 旧角色模型和语音样本

- `resources/live2d/maho-l2d/`
- `resources/live2d/MAY-l2d/`
- `resources/voice_sample_maho.wav`
- `resources/voice_sample_may.wav`

当前 `config.py` 默认使用 `resources/live2d/kurisu/amadeusV1.model3.json`，运行页面也固定加载 Kurisu 模型。上述旧角色资源没有找到当前执行路径的明确引用，设计稿也提到过它们可能被移除。建议先归档或备份，确认不再需要角色切换后再删除。

### 2. 旧版或重复音频

- `resources/voice_sample_clip_v2.wav`
- `resources/voice_sample.mp3`
- `resources/crs_1393.wav`
- `resources/crs_1393_clip.wav`
- `resources/reference_text.txt`

当前 GPT-SoVITS 客户端明确使用 `resources/crs_1393_clip.wav`；阿里云 TTS 设置会根据 `config.py` 的 `ref_audio` 使用 `resources/crs_1393.wav`。`voice_sample_clip_v2.wav` 仍出现在 PRD 和历史方案中，但不是当前默认路径。`voice_sample.mp3` 仍由角色配置指定，不能仅因为暂时没有直接读取就删除。建议先统一本地 TTS 和云端 TTS 的参考音频职责。

### 3. 未找到当前 UI 引用的图片

- `resources/images/mail_phone_frame.png`
- `resources/images/mail_phone_reference.jpeg`
- `resources/images/meswin.png`
- `resources/images/meswinLeft.png`
- `resources/images/meswinRight.png`
- `resources/login_button.png`
- `resources/terminal_back_0.png`
- `resources/thumb-1920-332314.jpg`
- `resources/resources/background_clothing_conf/`

在当前 Python、HTML 和 PyInstaller 配置中，静态搜索实际找到的运行时引用主要是 `bg.png`、`bgLogin.jpg`、`terminal_back.png`、Live2D 纹理和 icons。以上候选暂未发现直接运行时引用，但这不能排除手动演示、动态路径或外部配置引用；建议通过一次 UI 回归确认后再删除。

### 4. Live2D 旧入口和备份

- `resources/live2d/index.html`
- `resources/live2d/kurisu/motions/idle.motion3.json.bak`

当前窗口使用 `live2d/live2d_page.html` 和 `live2d/phone_live2d_page.html`；在当前 Python、HTML 和 PyInstaller 配置中，静态搜索没有发现直接加载 `resources/live2d/index.html`。`.bak` 是明确的备份候选，但仍应确认是否需要作为回退版本。

## 四、开发稿和探测文件

- `phone_mockup.html`：文件自身标明为设计草稿，当前运行使用 `live2d/phone_live2d_page.html`；可以考虑移到 `docs/archive/`。
- `splash.html`：在当前入口、PyInstaller spec 和代码中没有找到静态引用；属于高疑似冗余候选，但需要确认是否用于手动演示。
- `_probe_render.py`：在正式入口和测试中没有找到静态引用；不再做渲染诊断时可以归档。
- `build.txt`：在代码中没有找到静态引用，可能只是人工构建标记；先确认发布流程是否读取它。
- `docs/superpowers/`、`PRD_*.md`、`lessons.md`、`team-docs/`：属于设计、需求和经验资料，不能按“没有被 import”判断冗余。建议按“已实现、待实现、已取消”分类归档。

## 五、不能直接判定为冗余的目录

- `deepseek-harness-master/`：`core/harness_bridge.py` 会加载其中的 SDK 和 runtime，`Amadeus.spec` 也会打包相关内容；除非改成外部依赖，否则不能删除。
- `GPT-SoVITS/`：`desktop_pet.py` 会查找并自动启动本地 GPT-SoVITS API；仍需要本地 TTS 时不能删除。
- `Kurisu-GPT-SoVITS v2ProPlus Model/`：包含模型权重、参考音频和校验资料；改用纯云端 TTS 前不能删除。
- `resources/`、`live2d/`、`core/`、`ui/`、`tests/`：被入口、配置、测试或 PyInstaller spec 使用，不能整体删除。

## 六、建议清理顺序

1. 先制作可恢复的压缩备份，记录当前启动方式、TTS provider 和 Harness 模式。
2. 优先清理 `__pycache__/`、`.pytest_cache/`、空日志，再处理 `build/`、`dist/`。
3. 确认 `.venv/`、两个 GPT-SoVITS 虚拟环境和 Node 依赖中哪一套仍在使用，再逐个清理。
4. 运行测试并启动一次桌宠，验证 Kurisu Live2D、TTS、电话模式、Harness 和设置页面。
5. 最后处理 `splash.html`、`_probe_render.py`、`phone_mockup.html`、旧角色资源和未引用图片；建议先归档，不要直接永久删除。

## 七、工作树说明

截至本次核查，工作树状态如下：已有未提交修改的文件为 `desktop_pet.py`、`tests/test_agent_terminal.py`、`tests/test_crt_title_bar.py` 和 `ui/widgets/crt_title_bar.py`；另有新增测试 `tests/test_harness_terminal_bridge.py`，以及本文件本身。`core/harness_bridge.py`、`ui/settings_dialog.py`、`phone_mockup.html` 和 `live2d/phone_live2d_page.html` 当前未显示为 Git 修改文件。本次核查没有执行删除操作，也没有修改上述既有工作内容。
